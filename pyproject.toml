[project]
name = "learn-langgraph"
version = "0.1.0"
description = "LangChain -> LangGraph 學習筆記本"
requires-python = ">=3.12,<3.13"
dependencies = [
    "langchain>=0.3",
    "langgraph>=0.2",
    "langchain-openai>=0.2",
    "jupyter>=1.0",
    "python-dotenv>=1.0",
    "ipywidgets>=8.0",
    "langgraph-checkpoint-sqlite>=2.0",
    "langsmith>=0.12",
    "langchain-mcp-adapters>=0.3",
    "langchain-anthropic>=0.3",
    "anthropic>=0.40",
    "numpy>=1.26",
]
